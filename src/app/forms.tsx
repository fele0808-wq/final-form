import SectionScreen from '@/components/section-screen';

export default function FormsScreen() {
  return <SectionScreen name="Forms" description="Create, organize, and share every form in one place." />;
}