import SectionScreen from '@/components/section-screen';

export default function TrainScreen() {
  return <SectionScreen name="Train" description="Build consistency with focused practice and repeatable routines." />;
}