import SectionScreen from '@/components/section-screen';

export default function SettingsScreen() {
  return <SectionScreen name="Settings" description="Shape final form around the way you work." />;
}