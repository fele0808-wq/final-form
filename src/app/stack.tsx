import SectionScreen from '@/components/section-screen';

export default function StackScreen() {
  return <SectionScreen name="Stack" description="Keep your tools, resources, and progress together." />;
}