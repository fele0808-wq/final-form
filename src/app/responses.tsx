import SectionScreen from '@/components/section-screen';

export default function ResponsesScreen() {
  return <SectionScreen name="Responses" description="See what people are saying and keep every answer close." />;
}