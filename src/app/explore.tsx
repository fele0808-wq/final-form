import SectionScreen from '@/components/section-screen';

export default function ExploreScreen() {
  return <SectionScreen name="Explore" description="Find a starting point for your next form." />;
}
