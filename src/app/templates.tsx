import SectionScreen from '@/components/section-screen';

export default function TemplatesScreen() {
  return <SectionScreen name="Templates" description="Start faster with thoughtful forms for common workflows." />;
}